# Migration `watch-20191120163347`

This migration has been generated by Nikolas Burk at 11/20/2019, 4:33:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Post" (
  "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL,
  "content" text   ,
  "id" text NOT NULL  ,
  "published" boolean NOT NULL DEFAULT false ,
  "title" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191120162810..watch-20191120163347
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://nikolasburk:nikolasburk@localhost:5432/goto-demo"
 }
 generator photonjs {
   provider = "photonjs"
@@ -10,5 +10,14 @@
 model User {
   id    String  @id @default(cuid())
   name  String?
   email String  @unique
+  posts Post[]
+}
+
+model Post {
+  id        String  @id @default(cuid())
+  title     String
+  content   String?
+  published Boolean @default(false)
+  author    User
 }
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191120163347)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191120163347'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
