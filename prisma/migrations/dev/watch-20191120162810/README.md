# Migration `watch-20191120162810`

This migration has been generated by Nikolas Burk at 11/20/2019, 4:28:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
  "email" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "name" text   ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191120162810
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://nikolasburk:nikolasburk@localhost:5432/goto-demo"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+model User {
+  id    String  @id @default(cuid())
+  name  String?
+  email String  @unique
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191120162810)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191120162810'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
